services:
  backend:
    build: ./backend
    container_name: job-aggregator-api
    environment:
      - DATABASE_URL=sqlite:////app/data/jobs.db
      - CORS_ORIGINS=http://localhost:5173
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped